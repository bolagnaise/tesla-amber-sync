# .env.example
# Copy this file to .env and fill in your actual values

# Flask Configuration
# Generate a secure random string for production: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-super-secret-and-long-random-string-change-this
FLASK_RUN_PORT=5001

# Database (OPTIONAL - SQLite is used by default)
# SQLite (default): sqlite:////app/data/app.db (Docker) or ./data/app.db (local)
# PostgreSQL example: postgresql://user:password@localhost:5432/teslasync
# DATABASE_URL=postgresql://your_db_user:your_db_password@localhost:5432/teslasync

# Encryption Key (OPTIONAL - Auto-generated on first startup if not provided)
# The app will automatically generate and save an encryption key to data/.fernet_key
# Only set this if you want to use a specific key (e.g., for migrating from another instance)
# To generate manually: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# FERNET_ENCRYPTION_KEY=your-manually-generated-key-here

# ============================================================================
# API Credentials - Configure via Web UI
# ============================================================================
# All API credentials (Amber, Teslemetry, Tesla Energy Site ID) are configured
# through the web interface at Settings > API Configuration
# No environment variables needed for API access
