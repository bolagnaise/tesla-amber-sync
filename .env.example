# .env.example
# Copy this file to .env and fill in your actual values

# Flask
SECRET_KEY=your-super-secret-and-long-random-string-change-this
FLASK_RUN_PORT=5001

# Database (SQLite is used by default - this PostgreSQL URL is optional)
DATABASE_URL=postgresql://your_db_user:your_db_password@localhost:5432/teslasync

# Encryption Key (OPTIONAL - Auto-generated on first startup if not provided)
# The app will automatically generate and save an encryption key to /app/data/.fernet_key
# Only set this if you want to use a specific key (e.g., for migrating from another instance)
# To generate manually: from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())
# FERNET_ENCRYPTION_KEY=your-manually-generated-key-here

# Tesla API Credentials (OPTIONAL - Can be configured via web UI)
# These can be set via the Environment Settings page in the web interface
# Or you can set them here for convenience
# Get these from https://developer.tesla.com/
# TESLA_CLIENT_ID=your-tesla-client-id-from-developer-portal
# TESLA_CLIENT_SECRET=ta-secret.your-tesla-client-secret
# TESLA_REDIRECT_URI=http://localhost:5001/tesla-fleet/callback
# APP_DOMAIN=http://localhost:5001
